apiVersion: apps/v1
kind: Deployment
metadata:
  name: lithic-cli
  labels:
    app: lithic-cli
spec:
  replicas: 3
  selector:
    matchLabels:
      app: lithic-cli
  template:
    metadata:
      labels:
        app: lithic-cli
    spec:
      serviceAccountName: lithic-cli
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
      - name: lithic-cli
        image: lithic-cli:latest
        imagePullPolicy: Always
        env:
        - name: LITHIC_GRAPH_DIR
          value: "/app/graphify-out"
        - name: LITHIC_PROVIDER
          valueFrom:
            secretKeyRef:
              name: lithic-secrets
              key: provider
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: lithic-secrets
              key: openai-api-key
              optional: true
        - name: ANTHROPIC_API_KEY
          valueFrom:
            secretKeyRef:
              name: lithic-secrets
              key: anthropic-api-key
              optional: true
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - python3
            - /app/healthcheck.py
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command:
            - python3
            - /app/healthcheck.py
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: graph-storage
          mountPath: /app/graphify-out
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: graph-storage
        emptyDir:
          sizeLimit: 10Gi
      - name: tmp
        emptyDir:
          sizeLimit: 1Gi
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: lithic-cli
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: lithic-cli-netpol
spec:
  podSelector:
    matchLabels:
      app: lithic-cli
  policyTypes:
  - Egress
  egress:
  - to: []
    ports:
    - protocol: TCP
      port: 443  # HTTPS to LLM providers
    - protocol: TCP  
      port: 53   # DNS
    - protocol: UDP
      port: 53   # DNS